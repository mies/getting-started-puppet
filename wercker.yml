box : mies/puppet@0.0.2 
build :
  steps :
    - script:
        name : install manifest
        code : |
          sudo puppet apply --modulepath=$WERCKER_SOURCE_DIR/modules $WERCKER_SOURCE_DIR/manifests/init.pp
